{
  "name": "ocamlcalc",
  "version": "1.0.0",
  "private": true,
  "description": "OCamlCalc — exact financial calculator. Core engine written in OCaml, compiled to JavaScript with js_of_ocaml. No floats, no runtime deps.",
  "scripts": {
    "build:engine": "bash scripts/build_engine.sh",
    "gen:goldens": "python scripts/gen_goldens.py",
    "test": "node --test tests/*.test.mjs"
  }
}
